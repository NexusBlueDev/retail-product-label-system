{
  "name": "retail-product-label-system",
  "version": "3.4.0",
  "private": true,
  "description": "Mobile-first AI-powered product scanner for retail inventory",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "deploy:function": "supabase functions deploy extract-product --no-verify-jwt --project-ref ayfwyvripnetwrkimxka",
    "deploy:ls-upsert": "supabase functions deploy ls-upsert --no-verify-jwt --project-ref ayfwyvripnetwrkimxka",
    "lint": "echo 'Static HTML/ES6 app — no lint configured' && exit 0",
    "typecheck": "echo 'Static HTML/ES6 app — no TypeScript' && exit 0"
  },
  "devDependencies": {
    "supabase": "^2.93.0",
    "typescript": "^6.0.3"
  }
}
